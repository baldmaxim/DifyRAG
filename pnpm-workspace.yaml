packages:
  - 'apps/*'
  - 'packages/*'

# Native packages allowed to run install scripts (binaries required at build/test time).
allowBuilds:
  '@scarf/scarf': false
  '@swc/core': true
  esbuild: true
